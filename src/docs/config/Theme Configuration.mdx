import GenerateTheme from "./GenerateTheme/GenerateTheme";

# Generate Theme

You can generate a custom theme using the tool below. Select the colors you desire, then select the code button to generate the content.

You will need to replace the content of src/lib/styles/loading/loading.css if you want to keep these colors in the project.

You can also generate this from the demo site and add it a a live site if you desire.

<GenerateTheme />
